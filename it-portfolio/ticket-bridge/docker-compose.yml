version: "3.9"
services:
  ticket-bridge:
    build: .
    image: ticket-bridge:dev
    ports:
      - "8080:8080"
    environment:
      - TICKET_BRIDGE_CONFIG=/app/config.sample.yml
    # volumes:
    #   - ./it-portfolio/ticket-bridge/config.sample.yml:/app/config.sample.yml:ro